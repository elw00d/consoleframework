Roadmap

2. Сделать CheckBox

Подумать, как разделить элементы управления, которые могут содержать 1 дочерний элемент и те, которые являются
менеджерами размещения, то есть могут содержать множество дочерних элементов.

Запретить вызывать напрямую AddChild у Window и других контролов, которые являются конечными.

Сделать вспомогательную функцию getWindowsHost(), которая бы позволяла быстро получить WIndowsHost
из любого контрола;

Добавить в WindowsHost API для создания модальных окошек. Заодно сделать реализации стандартных
MessageBox для удобства. Когда модальное окно показывается, щелчки мышкой должны обрабатываться
WindowsHost'ом так, чтобы при PreviewMouseDown/PreviewMouseUp на области за модальным окном
args.Handled выставлялся в true, что предотвращало бы генерацию соответствующих Bubbling-событий.

Добавить функцию автоматического определения системы, под которой запускается приложение.

Добавить функцию проверки видимости точки контрола - не перекрывается ли она другим контролом.

Добавить assertions 

Добавить bindings